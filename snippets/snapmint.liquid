<!-- Begin Shopify-payment JavaScript -->
<script type="text/javascript">
  var mid = 1551;
  //1. Add this if need to show popup on cart page
    	var snapCartPrice = {{ cart.total_price | json }}
  //This will be the element class name where we want to insert the UI
    	var snapCartElName = '.subtotal'
 
  //2. Add this if need to show popup on sidebar cart
      var snapCartPrice = {{ cart.total_price | json }}
  //This will be the element class name where we want to insert the UI
      var snapCart = '.total-shipping'
      
  //3. Add this if need to show popup on product page
    	var snapProdDetail = {{ product | json }}
  //This will be the element class name where we want to insert the UI    
  var snapProdElName = '.price-container'

  //targeted price 
  var productTargetedPrice = '.price-container'
  var productTargetedPriceVariant = '.price-container'


</script>
<script src="https://assets.snapmint.com/assets/merchant/1551/snm_shopify_popup_min.js"></script>
<!-- End Shopify-payment JavaScript -->
